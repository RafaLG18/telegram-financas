apiVersion: v1
kind: Service
metadata:
  name: {{ include "caderneta.fullname" . }}
  labels:
    {{- include "caderneta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: health
      protocol: TCP
      name: health
  selector:
    {{- include "caderneta.selectorLabels" . | nindent 4 }}
