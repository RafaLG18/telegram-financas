{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "caderneta.serviceAccountName" . }}
  labels:
    {{- include "caderneta.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# The bot does not talk to the Kubernetes API.
automountServiceAccountToken: false
{{- end }}
